dp PC (
	out pc_out : ns(5)
){
	reg PCr : ns(5);
	always {
		//$display($dec, "Cycle: ", $cycle); // just for debug
		$display($dec, "pc_out:", pc_out); // just for debug
		// Insert your code here!
		pc_out = PCr;
		PCr = pc_out + 1;
	}
}

system PCsystem{
	PC(pc_out);
}
